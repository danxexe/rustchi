#[derive(Debug, Clone, Copy)]
pub enum IdentU4 {
    A,
    B,
    MX,
    MY,
    MSP,
    XP,
    XH,
    XL,
    YP,
    YH,
    YL,
    F,
}

// #[derive(Debug, Clone, Copy)]
// pub enum IdentU8 {
//     XHL,
//     YHL,
// }

#[derive(Debug, Clone, Copy)]
pub enum IdentU12 {
    X,
    Y,
}
